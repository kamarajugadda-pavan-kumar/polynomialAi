<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Google drive Plynomial AI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/app.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet"
        href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
</head>

<body>
    <!-- total screen -->
    <section class="w-screen h-screen  flex">
        <!-- side bar -->
        <div class="w-1/5 border-r flex flex-col">
            <!-- icon section -->
            <div class="h-16 flex place-content-center items-center border-b">
                <img class="h-1/2 w-1/7" src="/imgs/drive.png">
                <span class="ml-2 font-bold">google drive</span>
            </div>

            <!-- side bar menu section -->
            <div class="flex flex-col h-full place-content-center space-y-5">
                <a class="h-1/5 w-1/2 border py-12 text-center mx-auto hover:bg-black hover:text-white"
                    href="/upload">upload</a>
                <a class="h-1/5 w-1/2 border py-12 text-center mx-auto hover:bg-black hover:text-white"
                    href="/view-files">View Files</a>
            </div>
        </div>

        <!-- main content -->
        <div class="container w-full h-full flex flex-col">
        <% if(user){ %>
            <div class="ml-auto mr-6 bg-blue-800 text-white p-2 rounded-full mt-6"><%=user.username%></div>
            <div class="ml-auto mr-6 bg-blue-800 text-white p-2 rounded-full mt-6"><a href="/logout">Logout</a></div>
        <%}else{%>
            <div class="ml-auto mr-6 bg-blue-800 text-white p-2 rounded-full mt-6"><a href="/">login</a></div>
        <% }%>
        <div class="w-auto">
            <%-body-%>
        </div>
        </div>
    </section>
    <script src="/js/app.js"></script>
</body>

</html>